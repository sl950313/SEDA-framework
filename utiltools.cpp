#include "utiltools.h"

std::list<char *> split_string(char *src, char *tail, char token) {
   std::list<char *> _l;
   char *t_src = src;
   while (t_src != tail) {
      if (*t_src == token) {
         
      }
   }
   return _l;
}
